# 看板备注（generate_dashboard.py 自动读取 · 可编辑）

说明：结构化数据全在 CSV，这里只放 CSV 表达不了的"解释性文本"。
- 每节一行一条；`#` 开头为注释；日期用 `M/D` 格式。
- 「未完日期」「今日额外说明」仅对仍在 7 天滚动窗口内的日期生效，滚出窗口自动忽略。
- 日常报餐/称重不需要动这个文件；有特殊说明时再改。

## 未完日期
8/22: 午餐外出没来得及记录

## 今日额外说明
8/27: 这是示例数据，替换成你自己的记录即可

## 明细底部说明
示例数据集：覆盖热量4色（超量/接近/偏低）+ 缺餐 + 加餐场景

## 身体数据脚注
示例：体脂秤晨起空腹实测，时间以实际为准。

## 运动脚注
示例：手环/APP 记录；备注里写"X大卡"或"按X记"会被自动解析，没写则按时长×10 估算。
